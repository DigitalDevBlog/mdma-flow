# CLAUDE.md

@AGENTS.md

`AGENTS.md` is the authoritative file for this repository and is read by every harness. Keep this
file as a pointer to it rather than duplicating the rules, so the two cannot drift apart.
